---
"@trycourier/react-designer": minor
---

Add custom variable validation with configurable behavior. New `variableValidation` prop on TemplateEditor and BrandEditor allows restricting which variable names are allowed, with options for invalid behavior (`mark` or `remove`) and toast notifications via `invalidMessage`.

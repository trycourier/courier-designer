---
"@trycourier/react-designer": minor
---

Add Filled/Outlined button style toggle to the Inbox channel sidebar, including a visible border for the outlined variant and consistent button sizing whether one or two buttons are enabled. Round-trip the visual style through the Elemental `action.style` ("button"/"link") field so the backend renders the chosen variant.

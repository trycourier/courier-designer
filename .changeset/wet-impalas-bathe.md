---
"@trycourier/react-designer": minor
---

Add `duplicateTemplate` function to `useTemplateActions` hook for creating copies of templates. Supports both quick duplication (auto-generates `{templateId}-copy` name) and custom template IDs.

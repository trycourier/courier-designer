---
"@trycourier/react-designer": minor
---

Disable all variable functionality when `variables` prop is not provided to `TemplateEditor` — the variable toolbar button is hidden and typing `{{` no longer creates variable chips. Pass `variables={{}}` to enable variables without autocomplete suggestions.
